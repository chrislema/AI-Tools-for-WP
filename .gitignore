# Dependencies
node_modules/

# Build artifacts are INCLUDED (not ignored) so users can download and use directly
# Only ignore the .gitkeep placeholder
editor/build/.gitkeep

# IDE and OS files
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Environment files
.env
.env.local
.env.*.local

# WordPress uploads (if testing locally)
uploads/

# Composer (if used later)
vendor/
composer.lock
